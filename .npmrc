# Force npm to be used instead of other package managers
engine-strict=true
package-lock=true

# Include settings (newer npm config)
include=dev

# Registry and security
registry=https://registry.npmjs.org/
audit-level=moderate

# Install configuration
save=true
save-exact=false
fund=false
prefer-offline=false